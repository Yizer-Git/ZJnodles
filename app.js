App({
    // 全局数据：在这里配置 apiBaseUrl（替换为真实后端地址）
    globalData: {
        // 示例：在本地调试时可填 http://localhost:3000 或你的测试后端地址
            // apiBaseUrl: 'https://api.example.com'
    },
    onLaunch() {
        console.log('App launched');
    }
});
